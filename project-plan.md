# 33 岁生日文字冒险：项目规划

## 项目目标

- 形式：无选项、单线推进的动态视觉小说。
- 目标时长：10 至 15 分钟。
- 当前规模：28 个生产单元，111 个文本框。
- 视觉：实景像素混合，静态 CG 为主，有限动态为辅。
- 正式平台：macOS、Windows。
- 预览平台：Web，仅用于内部审阅，不作为首发硬指标。
- 工期：前置准备完成后，三周完成可发布版本。

## 技术方案

### 引擎

使用 Godot 4.7.1 stable，脚本语言使用 GDScript。

本机现有 `/Applications/Godot_mono.app` 为 Godot 3.2.3，不用于本项目；另行安装 Godot 4.7.1 标准版，不覆盖旧版本。

选择原因：

- `Control` 节点适合构建固定画幅的视觉小说界面。
- `AnimationPlayer`、`Tween` 和 CanvasItem Shader 可以完成视差、遮罩、像素溶解和有限动态。
- `VideoStreamPlayer` 可以直接承载 31 岁原片、旅行素材和检索录屏。
- 自定义转场和实景像素混合不受视觉小说模板限制。
- 可以直接导出 macOS 和 Windows 桌面版本。

技术边界：

- 不使用 C#，避免 .NET 依赖和 Web 导出限制。
- 不引入第三方对话、任务或视觉小说框架。
- 不使用 3D 场景。
- 不建立玩法、分支和状态机系统。
- Godot 核心视频格式使用 Ogg Theora；入选视频统一离线转码为 `.ogv`。

### 发布策略

- 首发只保证 macOS、Windows 桌面版。
- Web 版在桌面版完成后尝试构建。
- Web 视频播放性能、音视频同步或首次加载不达标时，直接取消 Web 发布。

## 工程目录

Godot 工程放在生日子项目内部：

`/Users/dtysky/Projects/dtysky/Pothos/projects/project-birthday-33/game`

规划目录：

```text
game/
├── project.godot
├── src/
│   ├── main.tscn
│   ├── story_controller.gd
│   ├── asset_registry.gd
│   ├── save_service.gd
│   ├── audio_service.gd
│   ├── ui/
│   │   ├── title_screen.tscn
│   │   ├── dialogue_box.tscn
│   │   ├── history_panel.tscn
│   │   ├── settings_panel.tscn
│   │   └── pause_menu.tscn
│   └── effects/
│       ├── pixel_dissolve.gdshader
│       ├── parallax_layer.gd
│       └── transition_layer.gd
├── content/
│   ├── story.json
│   └── asset_manifest.json
├── assets/
│   ├── backgrounds/
│   ├── characters/
│   ├── cg/
│   ├── overlays/
│   ├── ui/
│   ├── movies/
│   ├── audio/
│   │   ├── music/
│   │   ├── ambience/
│   │   ├── sfx/
│   │   └── voice/
│   └── fonts/
├── docs/
│   └── build.md
├── tests/
│   ├── validate_story.gd
│   └── smoke_test.gd
└── tools/
    └── transcode_video.sh
```

## 运行结构

主场景使用固定的 `Control` 节点树：

```text
Main
├── BackgroundLayer
├── VideoLayer
├── CharacterLayer
├── EffectsLayer
├── DialogueLayer
├── TransitionLayer
└── StoryController
```

`StoryController` 按 `content/story.json` 中的顺序推进：

```text
G1-01 -> ... -> G1-06
      -> G2-01 -> ... -> G2-06
      -> G3-01 -> ... -> G3-06
      -> G4-01 -> ... -> G4-05
      -> G5-01 -> ... -> G5-05
      -> ending
```

实现原则：

- 不建立分支系统。
- 不建立任务、背包、成就、数值和地图。
- 只使用一份 `story.json`，不增加数据库或自定义 DSL。
- 场景编号与 `game-scenes.md` 保持一致，例如 `G3-04` 对应一个 story unit。
- 每个生产单元可以连续显示多条对白，但只能使用一组主要画面资产。
- 存档只保存当前单元、当前文本框、设置和已读记录。
- 历史记录只保存在当前运行与存档数据中。

## 界面

只实现以下界面：

- 标题页：开始、继续、设置、退出。
- ADV 文本框：角色名、正文、点击继续提示。
- 快捷栏：历史、自动、快进、保存、读取、设置。
- 历史记录。
- 设置：文字速度、自动播放速度、音乐、音效、语音音量、全屏。
- 章节间黑屏标题。
- 结尾中央文字。

不实现：

- 角色图鉴、CG 鉴赏、音乐鉴赏。
- 多周目和章节选择。
- 多档位缩略图存档。
- 复杂粒子交互和可操作场景。

## 美术资产预算

### 新制资产

- 基础背景：`B00` 至 `B05`，共 6 组。
- 我的状态：`P01` 至 `P10`，优先由 1 套统一人设生成差分。
- Ousia：`A02` 至 `A04`，正面、坐姿、侧面与近距离状态。
- Agent：`A01` 一张群像，不分别制作动态立绘。
- 环境人物：`E01` 一组无脸剪影。
- UI：标题页、文本框、角色名标签、快捷栏、章节标题。
- 图层素材：数字、细线、文字页、光束、时间线、照片、风雪、迷雾、像素遮罩。

### 真实素材

- `R01` 真实聊天界面。
- `R02` 卧室吊灯。
- `R03` 二十秒检索录屏。
- `R04` 31 岁作品结尾。
- `R05` 埃及、新西兰。
- `R06` 国内道路与风光。
- `R07` 子梅垭口过程。
- `R08` 日落、星空、月升。
- `R09` 结尾真实道路。

### 文件规格

- 背景和 CG：1920 × 1080，PNG 或高质量 WebP。
- 透明人物和图层：PNG，保留 Alpha。
- 全屏视频：1920 × 1080，统一帧率，Ogg Theora（`.ogv`）。
- 音频母带：WAV；游戏内使用 Opus 或 OGG。
- 文件名使用小写英文、数字和下划线，不使用空格。
- 所有素材先登记到 `content/asset_manifest.json`，再进入游戏目录。

## 动态策略

优先级从高到低：

1. 静态 CG。
2. Godot `AnimationPlayer` 制作的眨眼、呼吸和轻微循环。
3. `Tween` 完成镜头推拉与分层视差。
4. CanvasItem Shader 完成像素溶解和局部显露。
5. `VideoStreamPlayer` 播放全屏真实视频。

禁止作为关键路径：

- AI 生成人物行走。
- AI 生成复杂口型。
- 多人连续动作。
- 精确手部交互。
- 依赖 AI 保持物件和人物连续性的长转场。

所有复杂转场必须有静态退化方案。Shader 或视频失败时，直接使用淡入淡出、遮罩或静态差分，不影响剧情完整播放。

## 音频方案

第一版默认不做全角色配音。

- 对白以文本框为准。
- G4 使用原片声音和现场声音。
- G3 的真实检索保留原始系统声音或环境声。
- 每章只配置一条基础氛围和少量转场音效。
- 最终决定配音时，优先录制“我”和 Ousia 的核心段落，不要求 Agent 全员配音。

## 制作流程

每个单元依次经过：

1. 台本锁定。
2. 资产编号确认。
3. 草图或真实素材选定。
4. 占位图进入游戏。
5. 对白、停留时间和转场可完整播放。
6. 正式美术替换占位图。
7. 有限动态与后期效果加入。
8. 单元验收后不再修改结构。

原则：

- 代码不等待正式美术。
- 正式美术不进入未锁定对白的单元。
- 动态不阻塞静态版本完成。
- 每晚都应有一个可以从头播放到当前进度的构建。

## 三周排期

### 前置阶段：2 至 3 天

- 安装 Godot 4.7.1 标准版，创建 Godot 工程和 Git 仓库。
- 实现标题页、文本框、历史和基础设置。
- 建立 `StoryController`、`story.json` 和 `asset_manifest.json`。
- 将 28 个生产单元和 111 个文本框全部导入占位版本。
- 完成 G1 垂直切片：占位背景、人物差分、转场、声音均可工作。
- 你女朋友确认角色比例、文本框遮挡范围和第一张关键 CG。

验收：可以从标题页完整播放 G1，不缺字、不溢出、不依赖正式美术。

### 第一周：主体画面

- 锁定全部对白。
- 完成“我”和 Ousia 的人设。
- 完成 Agent 群像和 6 组基础背景。
- G1、G2、G3 替换为正式或接近正式的美术。
- 整理 `R01` 至 `R04` 真实素材。

验收：G1 至 G3 可连续播放；所有关键对白都有最终构图。

### 第二周：真实素材与全片连通

- 整理 `R05` 至 `R09`，填写文件名和时间码。
- 完成 G4、G5。
- 完成像素覆盖、遮罩、视差和基础环境动态。
- 接入音乐、环境声、原片声音和现场声音。
- 形成第一版完整成片时长。

验收：游戏从开始到结尾可完整播放，没有占位文字和缺失素材。

### 第三周：收束与发布

- 校正每个文本框的停留时间、断句和自动播放节奏。
- 统一像素颗粒、色彩、人物比例和界面样式。
- 处理视频编码、音量和转场闪帧。
- 完成 macOS、Windows 构建。
- 进行两轮完整播放测试和一次外部观看测试。
- 只修复影响理解、播放或风格统一的问题，不新增场景。

验收：桌面构建可发布；从干净存档开始完整播放两次无阻塞。

## 分工

### 你

- 决定最终对白和删改。
- 提供真实影像、旧作和检索记录。
- 审核人物是否像自己、判断情绪是否真实。
- 决定音乐、声音和最终发布版本。

### 你女朋友

- 提供“我”和 Ousia 的人物设定。
- 提供 Agent 群像设计逻辑。
- 提供基础背景和关键 CG 的构图草图。
- 审核 AI 扩图后的人物一致性与画面风格。

### Codex

- 创建和维护 Godot 工程。
- 将台本转换为 `story.json` 和可运行场景。
- 实现界面、资产加载、转场、视频与音频播放。
- 建立占位资产和替换流程。
- 运行无头校验、导入、构建、回归测试并修复问题。

### AI 美术

- 根据人设与分镜扩展正式背景、人物差分和关键 CG。
- 生成有限循环所需的起止状态。
- 不负责文字排版、精确转场和最终合成。

## 验收标准

### 剧情

- 五章顺序固定，没有选项和死路。
- 111 个文本框全部可进入历史记录。
- 核心对白没有因技术实现被截断或改写。

### 视觉

- 1920 × 1080 下没有文本遮挡人物关键表情。
- 同一角色跨场景保持可识别。
- 新制画面没有明显的行走、口型或手部生成错误。
- 像素与真实素材的切换遵循 G1 至 G5 逐渐松动的方向。

### 技术

- `story.json` 和 `asset_manifest.json` 校验通过。
- Godot 无头启动和资源导入无错误。
- macOS、Windows 构建成功。
- 从新游戏开始到结尾无崩溃、黑屏和缺失资源。
- 视频切换无明显闪帧，音量没有突变。
- 自动播放、历史、保存和读取正常。

## 砍项顺序

工期不足时按以下顺序删除，不动核心对白：

1. Web 版本。
2. 非核心环境动态。
3. Agent 专属动画，只保留群像点亮。
4. 人物呼吸和衣物循环。
5. 自定义标题页动画。
6. 非核心配音。

不可删除：

- G1 两种表演的并置。
- G2 道路仍然有效与 Agent 群像。
- G3 二十秒检索和完整因果链。
- G4 对 31 岁作品的回答。
- G5 像素退去与主动失去。

## 立即执行

1. 安装 Godot 4.7.1 标准版，创建独立 Godot 工程。
2. 用纯色背景和姓名标签完成 28 个单元的占位版本。
3. 实现 G1 垂直切片。
4. 同步整理真实素材清单。
5. 在 G1 通过后锁定 UI 与美术规格，再批量生产其余场景。
