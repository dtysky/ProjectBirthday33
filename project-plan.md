# 33 岁生日文字冒险：项目规划

## 项目目标

- 形式：无选项、单线推进的动态视觉小说。
- 目标时长：10 至 15 分钟。
- 当前规模：28 个生产单元，111 条台词。
- 视觉：写实场景与像素融合，动态 CG 为主，不直接拼接全屏实拍段落。
- 最终输出：3840 × 2160（4K UHD），Godot 使用 1920 × 1080 逻辑坐标。
- 正式平台：macOS、Windows。
- 预览平台：Web，仅用于内部审阅，不作为首发硬指标。
- 工期：前置准备完成后，三周完成可发布版本。

## 当前进度

- Godot 4.7.1 工程已经初始化，使用 GDScript。
- 28 个生产单元、111 条对白和 19 个镜头包映射已生成运行时 JSON。
- 标题页、占位镜头、场景气泡、无框旁白、逐字演出、自动播放、历史、保存读取和基础设置可以运行。
- 111 条台词已区分为 53 条人物对白、34 条场景自白、20 条跨场景旁白和 4 条屏幕文字。
- 早期原型曾通过数据校验、无头完整流程和 3840 × 2160 实际渲染检查；模块化重构后尚未重复运行。
- SHOT-01 已接入。下一步等待园区、工位参考与人工构图，再制作 SHOT-02 和 SHOT-19。

## 技术方案

### 引擎

使用 Godot 4.7.1 stable，脚本语言使用 GDScript。

本项目使用 `/Applications/Godot_mono.app` 中的 Godot 4.7.1 stable。

选择原因：

- `Control` 节点适合构建固定画幅的视觉小说界面。
- 1920 × 1080 逻辑坐标保持 UI 排版稳定，`canvas_items` 在 4K 窗口中等比渲染。
- `AnimationPlayer`、`Tween` 和 CanvasItem Shader 可以完成视差、遮罩、像素溶解和有限动态。
- `VideoStreamPlayer` 只承载画中检索证据或已经定稿的动态 CG，不直接播放全屏旅行实拍。
- 自定义转场和实景像素混合不受视觉小说模板限制。
- 可以直接导出 macOS 和 Windows 桌面版本。

技术边界：

- 不使用 C#，避免 .NET 依赖和 Web 导出限制。
- 不引入第三方对话、任务或视觉小说框架。
- 不使用 3D 场景。
- 不建立玩法、分支和状态机系统。
- Godot 核心视频格式使用 Ogg Theora；画中证据和最终动态 CG 统一离线转码为 `.ogv`。

### 发布策略

- 首发只保证 macOS、Windows 桌面版。
- Web 版在桌面版完成后尝试构建。
- Web 视频播放性能、音视频同步或首次加载不达标时，直接取消 Web 发布。

## 工程目录

Godot 工程放在生日子项目内部：

`/Users/dtysky/Projects/dtysky/Pothos/projects/project-birthday-33/game`

规划目录：

```text
game/
├── project.godot
├── src/
│   ├── main.tscn
│   ├── story_controller.gd
│   ├── asset_registry.gd
│   ├── save_service.gd
│   ├── audio_service.gd
│   ├── ui/
│   │   ├── title_screen.tscn
│   │   ├── dialogue_box.tscn
│   │   ├── history_panel.tscn
│   │   ├── settings_panel.tscn
│   │   └── pause_menu.tscn
│   └── effects/
│       ├── pixel_dissolve.gdshader
│       ├── parallax_layer.gd
│       └── transition_layer.gd
├── content/
│   ├── story.json
│   └── asset_manifest.json
├── assets/
│   ├── shots/
│   ├── evidence/
│   ├── video/
│   ├── ui/
│   ├── fx/
│   ├── audio/
│   │   ├── music/
│   │   ├── ambience/
│   │   ├── sfx/
│   │   └── voice/
│   └── fonts/
├── docs/
│   └── build.md
├── tests/
│   ├── validate_story.gd
│   └── smoke_test.gd
└── tools/
    └── transcode_video.sh
```

## 运行结构

主场景使用固定的 `Control` 节点树：

```text
Main
├── BackgroundLayer
├── VideoLayer
├── CharacterLayer
├── EvidenceLayer
├── EffectsLayer
├── DialogueLayer
├── TransitionLayer
└── StoryController
```

`StoryController` 按 `content/story.json` 中的顺序推进：

```text
G1-01 -> ... -> G1-06
      -> G2-01 -> ... -> G2-06
      -> G3-01 -> ... -> G3-06
      -> G4-01 -> ... -> G4-05
      -> G5-01 -> ... -> G5-05
      -> ending
```

实现原则：

- 不建立分支系统。
- 不建立任务、背包、成就、数值和地图。
- 只使用一份 `story.json`，不增加数据库或自定义 DSL。
- 场景编号与 `design/game-scenes.md` 保持一致，例如 `G3-04` 对应一个 story unit。
- 每个生产单元可以连续显示多条对白，但只能使用一组主要画面资产。
- 存档只保存当前单元、当前台词、设置和已读记录。
- 历史记录只保存在当前运行与存档数据中。

## 界面

只实现以下界面：

- 标题页：开始、继续、设置、退出。
- 场景对白与自白：无姓名标签、指向发声人物的悬浮气泡。
- 跨场景旁白：画面内无框字幕；旧作原文和屏幕文字使用中央文字。
- 收起式菜单：历史、自动、保存、读取、设置和返回标题。
- 历史记录。
- 设置：文字速度、自动播放速度、主音量、全屏。
- 结尾中央文字。

不实现：

- 角色图鉴、CG 鉴赏、音乐鉴赏。
- 多周目和章节选择。
- 多档位缩略图存档。
- 复杂粒子交互和可操作场景。

## 美术资产预算

### 新制资产

- 全局人设：我、Ousia、Agent 群像和一组低细节同事剪影。
- 服装：我的中性、工作、户外、居家和 31 岁旧作五套状态，以及 Ousia 的核心服装。
- 地点母版：`LOC-00` 至 `LOC-09`，共 10 组，只锁定空间、材质和光线规律。
- 镜头包：`SHOT-01` 至 `SHOT-19`，覆盖 28 个生产单元，合计约 27 个关键构图；每个构图先制作人物与环境一体的母帧。
- 拆层资产：仅为需要视差、表情差分、遮罩或局部动画的镜头制作。
- UI：标题页、对白气泡、无框字幕、中央文字、收起式菜单。
- 特效：数字、细线、文字页、光束、时间线、照片、风雪、迷雾和像素遮罩。

具体镜头包交付和生产单元映射以 `art/asset-list.md` 为准。地点母版不是可直接套用的背景图库，人物也不作为跨场景通用立绘生产。

### 来源素材

- `SRC-01` 至 `SRC-04`：聊天、吊灯、检索和 31 岁作品，作为叙事原件或抽帧重绘来源。
- `SRC-05` 至 `SRC-09`：旅行、道路和子梅垭口素材，只作为构图、天气、光线与事实顺序参考。

来源素材不直接作为全屏运行时资产。聊天和检索保留在画中屏幕，31 岁作品抽帧重绘并可保留原声音轨，其余素材全部转制为统一风格的动态 CG。

### 文件规格

- 镜头母帧：3840 × 2160，高质量 WebP 或 PNG。
- 拆层文件：与母帧同尺寸、像素对齐，透明人物和前景使用带 Alpha 的 PNG。
- 镜头目录：`assets/shots/shot-xx/`，不需要拆层的镜头只保留母帧。
- 画中证据：裁切后进入 `assets/evidence/`，通过遮罩限制在场景屏幕或投影区域。
- 最终动态 CG：3840 × 2160，统一帧率，Ogg Theora（`.ogv`），进入 `assets/video/`。
- 音频母带：WAV；游戏内使用 Opus 或 OGG。
- 文件名使用小写英文、数字和下划线，不使用空格。
- 所有素材先登记到 `content/asset_manifest.json`，再进入游戏目录。

## 动态策略

优先级从高到低：

1. 静态 CG。
2. Godot `AnimationPlayer` 制作的眨眼、呼吸和轻微循环。
3. `Tween` 完成镜头推拉与分层视差。
4. CanvasItem Shader 完成像素溶解和局部显露。
5. `VideoStreamPlayer` 播放画中证据或最终动态 CG。

禁止作为关键路径：

- AI 生成人物行走。
- AI 生成复杂口型。
- 多人连续动作。
- 精确手部交互。
- 依赖 AI 保持物件和人物连续性的长转场。
- 未经转制的全屏实拍旅行蒙太奇。

所有复杂转场必须有静态退化方案。Shader 或视频失败时，直接使用淡入淡出、遮罩或静态差分，不影响剧情完整播放。

## 音频方案

第一版默认不做全角色配音。

- 对白以场景气泡、无框旁白和中央文字三种呈现为准。
- G4-01 可以保留 31 岁原片声音，其余 G4 使用现场环境声作为声音素材。
- G3 的画中检索保留原始系统声音。
- 每章只配置一条基础氛围和少量转场音效。
- 最终决定配音时，优先录制“我”和 Ousia 的核心段落，不要求 Agent 全员配音。

## 制作流程

每个单元依次经过：

1. `design/game-scenes.md` 中的台本锁定。
2. 在 `art/asset-list.md` 中确认所属镜头包及需要的来源素材。
3. 完成场景草图和人物、环境一体的构图母帧。
4. 占位图进入游戏。
5. 对白、停留时间和转场可完整播放。
6. 母帧通过后按需拆层，并替换占位图。
7. 有限动态与后期效果加入。
8. 单元验收后不再修改结构。

原则：

- 代码不等待正式美术。
- 正式美术不进入未锁定对白的单元。
- 动态不阻塞静态版本完成。
- 每晚都应有一个可以从头播放到当前进度的构建。

## 三周排期

### 前置阶段：2 至 3 天

- 安装 Godot 4.7.1 标准版，创建 Godot 工程和 Git 仓库。
- 实现标题页、对白气泡、无框旁白、历史和基础设置。
- 建立 `StoryController`、`story.json` 和 `asset_manifest.json`。
- 将 28 个生产单元和 111 条台词全部导入占位版本。
- 完成 G1 垂直切片：占位背景、人物差分、转场、声音均可工作。
- 你女朋友确认角色比例、气泡留白范围和第一张关键 CG。

验收：可以从标题页完整播放 G1，不缺字、不溢出、不依赖正式美术。

### 第一周：主体画面

- 锁定全部对白。
- 完成“我”和 Ousia 的人设。
- 完成 G3 无 Agent 的独立开场、六名 Agent 的独立工作场景、G1 至 G3 所需地点母版，以及 G3 新增的 `SHOT-32` 至 `SHOT-42`。
- G1、G2、G3 替换为正式或接近正式的美术。
- 整理 `SRC-01` 至 `SRC-04`，完成画中证据裁切与旧作抽帧。

验收：G1 至 G3 可连续播放；所有关键对白都有最终构图。

### 第二周：旅行 CG 与全片连通

- 从 `SRC-05` 至 `SRC-09` 中选定构图参考，不按路线堆砌素材。
- 完成 `SHOT-12` 至 `SHOT-18`，连通 G4、G5。
- 完成像素覆盖、遮罩、视差和基础环境动态。
- 接入音乐、环境声、31 岁原片声音和检索系统声音。
- 形成第一版完整成片时长。

验收：游戏从开始到结尾可完整播放，没有占位文字和缺失素材。

### 第三周：收束与发布

- 校正每条台词的停留时间、视觉分页和自动播放节奏。
- 统一像素颗粒、色彩、人物比例和界面样式。
- 处理画中证据与动态 CG 的视频编码、音量和转场闪帧。
- 完成 macOS、Windows 构建。
- 进行两轮完整播放测试和一次外部观看测试。
- 只修复影响理解、播放或风格统一的问题，不新增场景。

验收：桌面构建可发布；从干净存档开始完整播放两次无阻塞。

## 分工

### 你

- 决定最终对白和删改。
- 提供实拍参考、旧作和检索记录。
- 审核人物是否像自己、判断情绪是否真实。
- 决定音乐、声音和最终发布版本。

### 你女朋友

- 提供“我”和 Ousia 的人物设定。
- 提供 Agent 群像设计逻辑。
- 提供地点母版和各镜头包的构图草图。
- 审核 AI 母帧中的人物一致性、场面关系与画面风格。

### Codex

- 创建和维护 Godot 工程。
- 将台本转换为 `story.json` 和可运行场景。
- 实现界面、资产加载、转场、视频与音频播放。
- 建立占位资产和替换流程。
- 运行无头校验、导入、构建、回归测试并修复问题。

### AI 美术

- 根据人设、地点母版与构图草图先生成完整镜头母帧。
- 母帧通过后补出需要的干净背景、人物差分、前景、遮罩和有限循环起止状态。
- 不负责文字排版、精确转场和最终合成。

## 验收标准

### 剧情

- 五章顺序固定，没有选项和死路。
- 111 条台词全部可进入历史记录。
- 核心对白没有因技术实现被截断或改写。

### 视觉

- 3840 × 2160 输出下没有文本遮挡人物关键表情，窗口缩放到 1920 × 1080 时仍可读。
- 同一角色跨场景保持可识别。
- 人物与环境在透视、光向、颗粒和接触关系上属于同一个画面。
- 新制画面没有明显的行走、口型或手部生成错误。
- 像素颗粒从 G1 至 G5 逐渐松动，但场景始终属于同一套 CG 美术。
- 聊天、检索和旧作原件只出现在有明确边界的画中区域。

### 技术

- `story.json` 和 `asset_manifest.json` 校验通过。
- Godot 无头启动和资源导入无错误。
- macOS、Windows 构建成功。
- 从新游戏开始到结尾无崩溃、黑屏和缺失资源。
- 画中证据与动态 CG 切换无明显闪帧，音量没有突变。
- 自动播放、历史、保存和读取正常。

## 砍项顺序

工期不足时按以下顺序删除，不动核心对白：

1. Web 版本。
2. 非核心环境动态。
3. Agent 专属动画，只保留群像点亮。
4. 人物呼吸和衣物循环。
5. 自定义标题页动画。
6. 非核心配音。

不可删除：

- G1 两种表演的并置。
- G2 道路仍然有效与 Agent 群像。
- G3 二十秒检索和完整因果链。
- G4 对 31 岁作品的回答。
- G5 像素退去与主动失去。

## 立即执行

1. 安装 Godot 4.7.1 标准版，创建独立 Godot 工程。
2. 用纯色背景和姓名标签完成 28 个单元的占位版本。
3. 实现 G1 垂直切片。
4. 同步整理来源素材清单，只选构图和叙事所需内容。
5. 在 G1 通过后锁定 UI 与美术规格，再批量生产其余场景。
